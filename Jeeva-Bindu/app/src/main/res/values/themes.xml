<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose is used for UI; this theme only supplies window-level defaults. -->
    <style name="Theme.JeevaBindu" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
